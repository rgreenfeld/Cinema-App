import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hardlifechannel.cinemaster',
  appName: 'CineMaster',
  webDir: 'dist',
};

export default config;
